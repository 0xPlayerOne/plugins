# Control Plane read-only alignment notes

The sibling `/Users/amf/Developer/AgentHQ/control-plane` repository was inspected
without modification. Its domain contracts model versioned skills, required
capabilities and tools, content digests, and immutable ExecutionPlan pins. This
repository documents that mapping in `control-plane-integration.md`; no sibling
repository files are changed by marketplace synchronization.
